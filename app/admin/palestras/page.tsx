import { PalestrasManager } from "@/components/admin/palestras/PalestrasManager";

export const metadata = { title: "Admin — Config. Palestra" };

export default function PalestrasPage() {
  return <PalestrasManager />;
}
